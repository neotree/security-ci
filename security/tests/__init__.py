"""Regression tests for the CI security gate.

This file exists so `python3 -m unittest discover -s security/tests -t .` can import
the package; without it, discovery fails with "Start directory is not importable".
"""
